<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OASISEX"
        tools:targetApi="31">

        <!-- <activity
            android:name=".Trips"
            android:exported="true">
            <- No intent-filter here means this activity is not a launcher activity
        </activity>-->
        <activity
            android:name=".mainPage"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DetailsPage"/>
        <activity android:name=".booking"/>
        <activity android:name=".PayPage"/>
        <activity android:name=".Account" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".signUp" />
        <activity android:name=".Trips" />
        <activity android:name=".HelpCentre" />
        <activity android:name=".EditPage" />
        <activity android:name=".MainActivity" />
        <activity android:name=".searchActivity" />




    </application>

</manifest>